import { FC } from "react";

export const GenerateBtn: FC = () => {
    return (
        <button className="w-full bg-succsess border border-primary mt-5 p-3 font-primary hover:border  hover:bg-primary hover:text-secondary ease-in-out duration-300 hover:duration-300 hover:ease-in-out">
            Generate
        </button>
    );
};
